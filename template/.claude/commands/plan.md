# /plan

Turn an approved design into a zero-context, testable task breakdown.

Read `docs/process/workflow.md` (Plan) and `docs/process/journal-state-plans.md`
for where the plan file lives. Produce bite-sized tasks with exact files, code,
and test commands — assume the implementer knows the toolchain but not this
codebase. Required for every Tier 3+ change (`docs/process/risk-tiers.md`).

Next: `/execute`.
