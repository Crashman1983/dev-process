# /review

The gate before merging to the main branch.

Read `docs/process/workflow.md` (Review); the required depth scales with the
tier (`docs/process/risk-tiers.md`). Judge functional completeness, correctness,
and rule adherence against the plan or spec. Fixes loop back through `/execute`
and `/review` until the branch is clean.
