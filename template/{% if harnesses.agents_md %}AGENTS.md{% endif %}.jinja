# {{ project_name }} — agent instructions

## Start

The authoritative process lives in `docs/process/`. If this repository has
just been initialized, start with `docs/process/start-here.md` and guide the
user through Greenfield or Brownfield as a dialogue.

<!-- KERNEL:START -->
## Always-on kernel

**Mandatory rules (full text: `docs/process/mandatory-rules.md`):**
1. Verify before asserting. 2. Plan before substantive work. 3. Contract/interface first.
4. One owner per behavior. 5. Tests prove acceptance. 6. Root cause before symptom.
7. Review gate before merge. 8. Atomic commits. 9. Readable, intention-revealing code.

**Tier routing (full matrix: `docs/process/risk-tiers.md`):**
Tier 0 -> direct commit. Tier 1-2 -> quick flow. Tier 3+ -> plan -> execute -> review.
Scope, not diff size, sets the tier.

**Language:** converse with the user in the user's language; write all artifacts
(docs, code, commits, ADRs, journal) in English.
<!-- KERNEL:END -->

## Process commands

Harnesses without a native command menu invoke a phase by reading the named
process file.

- **brainstorm** — idea -> approved design (`docs/process/workflow.md`)
- **plan** — design -> task plan (`docs/process/workflow.md`)
- **execute** — build the plan test-driven (`docs/process/workflow.md`)
- **review** — merge gate (`docs/process/workflow.md`)
- **quick** — Tier 1/2 shortcut (`docs/process/workflow.md`)
- **debug** — root cause before fix (`docs/process/workflow.md`)
- **commit** — atomic conventional commit (`docs/process/commits.md`)
- **prime** — restore context (`docs/process/journal-state-plans.md`)
