review: sp42-44-arc (v1.26.0-v1.30.0 span)
work: PR #23
issue: publish-waived: all findings fixed in-PR before merge (this report is the record)

## Prompt

```
Two independent merge-gate reviewers over the unreviewed span ba15e4c..HEAD
(kernel-integrity gate, compaction directive, profiles, branch-protection
script + job rename, trace.py). One adversarial/execution (defects proven by
reproduced command/output; mocked gh, rendered probes), one process-coherence
(contradictions and stale references against rendered output). (Model: Fable.
Full prompts archived in session.)
```

## Result

1 blocker, 1 major, 2 medium, several minors — all reproduced/evidenced, all
fixed in the same PR (v1.30.1). Held under attack: kernel byte-identity across
all four copies, profile derivation on all values (`profile=bogus` fails clean,
no half-render), the required-check chain end-to-end after the job rename,
CHANGELOG extraction lossless + chronologically repaired, trace crash-inputs
neutralized, version consistency.

## Findings

FINDING sev=blocker action=fix issue=- setup_branch_protection wiped an existing reviews-only protection rule: GET required_status_checks 404s ambiguously, any failure took the destructive PUT path; now the whole rule is probed, existing rules are never rewritten, only explicit "Branch not protected" creates
FINDING sev=major action=fix issue=- kernel gate false-green: a second tampered KERNEL block behind an intact first passed the first-match compare; now >1 START marker hard-fails (anchors and kernel.md)
FINDING sev=major action=fix issue=- BOOTSTRAP's pre-profile module heuristic duplicated the hardening ratchet (two owners); step 3 now points at the ratchet
FINDING sev=major action=fix issue=- ratchet covered 11 of 13 modules — doc_drift_gate and git_hooks had no documented path on minimal/custom; triggers added, test pins all 13
FINDING sev=minor action=fix issue=- `copier update --data profile=team` is a silent no-op leaving a lying answers file; update semantics now stated in profile help and BOOTSTRAP
FINDING sev=minor action=fix issue=- kernel gate accepted an empty canonical block (zero rules everywhere, green); rejected now
FINDING sev=minor action=fix issue=- trace bare-number commit grep over-matched (v0.42.1, PR-142 listed for #42); bare digit keys dropped from the grep
FINDING sev=minor action=fix issue=- trace blank query matched every plan/commit/report; refused with usage
FINDING sev=minor action=fix issue=- trace misnamed an unborn-branch repo as "git unavailable"; README module catalog missed github-master; Nutzung omitted the profile question; kernel wording invited an always-in-context overclaim; doubled REVIEW bullet; unguarded second gh call
FINDING sev=nit action=accept issue=- a literal fenced KERNEL-marker example in an anchor would false-red (no fence awareness); no rendered file trips it, adopters extend outside markers — accepted
FINDING sev=nit action=accept issue=- doc-drift will not catch a future rename of setup_branch_protection.sh (bare-filename reference by design); accepted as the cost of the ci-off-safe reference
