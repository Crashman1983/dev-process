# Plan: arch-docs module (SP15)

Design: `docs/design/2026-07-03-sp15-arch-docs-design.md`. New opt-in module,
mirrors the telemetry module's anatomy.

## Task 1 — module wiring

- `copier.yml` modules default: add `arch_docs: false`.
- `gate_runner.py.jinja` GATES: add
  `"arch-docs": ("arch_docs", [sys.executable, "scripts/process/check_arch_docs.py", "."])`.
- `tests/conftest.py` full modules dict: add `arch_docs: False`.

## Task 2 — the scaffold + gate + module doc

- `template/{% if modules.arch_docs %}ARCHITECTURE-OVERVIEW.md{% endif %}`:
  arc42/C4-lite sections (context, quality goals, runtime, deployment, risks &
  tech debt, glossary) + pointer sections (building blocks → ARCHITECTURE.md,
  decisions → docs/process/adr/), a verify-tag legend, placeholder markers.
- `template/scripts/process/{% if modules.arch_docs %}check_arch_docs.py{% endif %}`:
  hard-fail dead `ADR-NNNN` refs + non-UTF-8; soft-note remaining placeholders
  and file-absent. Pure stdlib.
- `template/docs/process/{% if modules.arch_docs %}modules{% endif %}/arch-docs.md`:
  arc42→section mapping, the structure/decisions/narrative one-owner split, the
  verify tags, what the gate does and does not check.

## Task 3 — upstream docs

- README module list + roadmap row SP15 + status; BOOTSTRAP two `--data` blocks;
  SYSTEM-REQUIREMENTS module table (stdlib); start-here module heuristic; SBOM
  version row + project row.

## Task 4 — tests + release

- `tests/test_arch_docs.py`: module on ships scaffold+gate+doc; module off ships
  nothing (incl. no overview); gate green on the seed; hard-fail on a dead ADR
  ref; soft-note placeholders; `gate_runner --list` includes `arch-docs`;
  neutrality; doc-drift green with the module.
- `pyproject.toml` → 1.5.0 (new module = minor); SBOM; README.
- Independent adversarial review (Tier-4 rule); attest single-family; merge +
  tag `v1.5.0`.
